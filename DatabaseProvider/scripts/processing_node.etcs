#!/usr/bin/env etcs
#IMPORT ACE_ROOT/modules

requires db

db.Connect(processing.db)
db.ExecuteRaw(PRAGMA journal_mode=WAL;)
db.InitializeSchema(CREATE TABLE IF NOT EXISTS users(id INTEGER PRIMARY KEY, name TEXT, role TEXT);)
db.InitializeSchema(CREATE TABLE IF NOT EXISTS audit(id INTEGER PRIMARY KEY, event TEXT);)